server {
  listen 80;
  server_name localhost;

  location / {
    root /usr/share/nginx/html;
    index index.html index.htm;
    # This is the key part for SPAs:
    # It will serve the file if it exists, otherwise fall back to index.html
    try_files $uri $uri/ /index.html;
  }
}